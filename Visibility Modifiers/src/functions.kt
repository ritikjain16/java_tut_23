public fun sum(a:Int,b:Int)=a+b

private fun difference(a:Int,b:Int)=a-b

//protected cannot be used as top level function.
//only used for functions inside the class

//protected fun multiply(a:Int,b:Int)=a*b

internal fun division(a:Int,b:Int)=a/b
